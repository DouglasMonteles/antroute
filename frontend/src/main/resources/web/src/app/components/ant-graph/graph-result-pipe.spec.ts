import { GraphResultPipe } from './graph-result-pipe';

describe('GraphResultPipe', () => {
  it('create an instance', () => {
    const pipe = new GraphResultPipe();
    expect(pipe).toBeTruthy();
  });
});
